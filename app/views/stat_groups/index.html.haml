%table.table.table-striped.table-hover
  %thead
    %tr
      %th Date
      %th Creator
      %th Actions
  %tbody
    - @stat_groups.each do |sg|
      %tr
        %td= sg.created_at.to_s
        %td= "Me"
        %td= link_to "Edit", edit_stat_group_path(sg)
